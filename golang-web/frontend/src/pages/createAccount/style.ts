import styled from 'styled-components';

const FormContainer = styled.form`
  button {
    margin: 1em 0;
  }
  label {
    p {
      margin: 1em 0 0 0;
    }
    display: block;
  }
`;

export { FormContainer };
